/**
 * Stub for the `server-only` package.
 *
 * The real module throws when imported outside a React Server Component, which
 * is exactly what we want in the app and exactly what we do not want in a Node
 * test runner. Aliased in vitest.config.ts.
 */
export {};
